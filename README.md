# Word to Excel Converter Tool

A simple Streamlit-based tool to extract tables from Word documents and save them as Excel files.

## How to Run (For Others)

1.  **Install Python**: Ensure Python 3.8+ is installed.
2.  **Install Dependencies**: Open a terminal in this folder and run:
    ```bash
    pip install -r requirements.txt
    ```
3.  **Start the Tool**: Double-click `run_tool.bat`.

## Online Deployment (GitHub + Streamlit Cloud) 🚀

This is the most professional way to share the tool. Your colleagues will access it via a URL, no installation required.

### Step 1: Upload to GitHub
1.  **Create a Repository**: Go to [GitHub](https://github.com/new) and create a new repository (can be Private).
2.  **Upload Files**: Upload the following files:
    - `app.py`
    - `requirements.txt`
    - `.streamlit/config.toml`
    - `README.md`
    - `.gitignore`

### Step 2: Connect to Streamlit Cloud
1.  Sign in to [Streamlit Cloud](https://share.streamlit.io/) with your GitHub account.
2.  Click **"New app"**.
3.  Select your repository, the main branch, and set the file path to `app.py`.
4.  Click **"Deploy!"**.
5.  Wait a minute, and you will get a permanent URL (e.g., `https://your-app-name.streamlit.app`) to share.

---

## Local Run (For Offline Use)

If you prefer to run it locally:
1.  Install Python 3.8+.
2.  Install dependencies: `pip install -r requirements.txt`.
3.  Double-click `run_tool.bat`.
